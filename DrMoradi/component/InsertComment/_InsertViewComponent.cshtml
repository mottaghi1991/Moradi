@using Core.Dto.ViewModel.main
@using Domain.Main
@using Core.Extention
@model CommentsViewModel
@section Style{
    <style>
   


    </style>
}
<div class="comment-thread">
    @{
        RenderComments(Model.Comments, null, 0);
    }
</div>

@functions {
    void RenderComments(IEnumerable<Comment> comments, int? parentId, int level)
    {
        var replies = comments.Where(c => c.ParentId == parentId).ToList();
        if (!replies.Any()) return;

        foreach (var comment in replies)
        {
            var bg = level switch
            {
                0 => "bg-white",
                1 => "bg-light",
                2 => "bg-light-subtle",
                _ => "bg-light-subtle"
            };

            <div class="comment-card @bg border rounded-3 p-3 mb-3">

                <!-- نام و تاریخ -->
                <div class="d-flex justify-content-between align-items-center mb-2 flex-wrap gap-2">
                    <div class="d-flex align-items-center gap-2">
                        <div class="comment-avatar rounded-circle d-flex align-items-center justify-content-center text-white fw-bold"
                             style="background-color:#f4d28a;width:36px;height:36px;">
                            @comment.Name.Substring(0,1)
                        </div>
                        <strong>@comment.Name</strong>
                    </div>
                    <small class="text-muted">@comment.CreateDate.ToPersian()</small>
                </div>

                <!-- متن -->
                <div class="comment-text mb-2">@comment.Text</div>

                <!-- دکمه پاسخ -->
                @* 
                @if (User.Identity?.IsAuthenticated == true)
                {
                    <button type="button"
                            class="btn btn-sm btn-link text-primary p-0"
                            onclick="openReplyModal(@comment.Id)">
                        🗨️ پاسخ
                    </button>
                }
                *@
                <!-- پاسخ‌ها -->
                <div class="comment-replies mt-3">
                    @{
                        RenderComments(comments, comment.Id, level + 1);
                    }
                </div>
            </div>
        }
    }
}


<!-- مودال بوت‌استرپ -->
<div class="modal fade" id="replyModal" tabindex="-1" aria-labelledby="replyModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" action="/Home/Replay">
                <div class="modal-header">
                    <h5 class="modal-title" id="replyModalLabel">ارسال پاسخ</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="بستن"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" name="ParentId" id="modalParentId" value="" />
                    <input type="hidden" name="EntityId" value="@Model.EntityId" />
                    <input type="hidden" name="EntityType" value="@Model.EntityType" />
                    <textarea name="Text" class="form-control" rows="3"  placeholder="پاسخ خود را بنویسید..."></textarea>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">بستن</button>
                    <button type="submit" class="btn btn-primary">ارسال پاسخ</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- استایل -->


<!-- اسکریپت برای باز کردن مودال -->
<script>
    function openReplyModal(parentId) {
        document.getElementById('modalParentId').value = parentId;
        var replyModal = new bootstrap.Modal(document.getElementById('replyModal'));
        replyModal.show();
    }
</script>
