<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>دکتر معصومه مرادی | متخصص تغذیه و رژیم درمانی</title>
    <link rel="icon" type="image/png" href="@Url.Content("~/FileUpload/Slider/logo-m.png")">  
    <script type="application/ld+json">
@Html.Raw(@"{
  ""@context"": ""https://schema.org"",
  ""@type"": ""WebSite"",
  ""name"": ""دکتر معصومه مرادی"",
                    ""alternateName"": ""دکتر معصومه مرادی"",
  ""url"": ""https://drmoradi-diet.com""
}")
</script>

    <!-- لینک به CSS بوت‌استرپ -->

@*     <link href="~/css/swiper.css" rel="stylesheet" />
    <link href="~/myfile/dist/css/custom-style.css" rel="stylesheet" asp-append-version="true" />
    <link href="~/common/sweetalert/sweetalert2.min.css" rel="stylesheet" />
    <link href="~/css/maincss.css" rel="stylesheet"  asp-append-version="true"/>
    <link href="~/css/bootstrap5.3.0.css" rel="stylesheet" />
 *@
    <link rel="stylesheet" href="~/scss/main.min.css" asp-append-version="true" />


    @await RenderSectionAsync("style", false)
    
</head>
<body data-isloggin="@User.Identity.IsAuthenticated.ToString().ToLower()">

    @await Component.InvokeAsync("PopUpComponent")
    <!-- هدر و ناوبار -->
    <!-- HTML –– درون <body> -->
    @await Component.InvokeAsync("NavbarComponent")
    <section style="padding-top:80px">
    @RenderBody()
    </section>







    <!-- فوتر -->
    @await Component.InvokeAsync("FooterComponent")

    <partial name="_footerlink" />
    <partial name="_Message" />
    <!-- اسکریپت‌های مورد نیاز بوت‌استرپ -->

    <partial name="_Message" />
    @await RenderSectionAsync("Scripts", required: false)
  

          <script>
        document.addEventListener("DOMContentLoaded", function () {
            if (!sessionStorage.getItem("popupShown")) {
                var popupElement = document.getElementById('popup');
                var popupModal = new bootstrap.Modal(popupElement);

                popupModal.show();
                sessionStorage.setItem("popupShown", "true");

                popupElement.addEventListener('hidden.bs.modal', function () {
                    document.querySelectorAll('.modal-backdrop').forEach(el => el.remove());
                    document.body.classList.remove('modal-open');
                    document.body.style.removeProperty('padding-right');
                    popupElement.remove();
                });
            }
        });
    </script>
</body>
</html>
